## Summary

<!-- What changed, and why. Link any issue this closes. -->

## Docs

<!-- Does this change observable behavior — a route, status code, error code,
     auth rule, or environment variable? If so, link the docs/ sections updated
     in this PR (docs/api.md, docs/deployment.md, README's config table,
     .env.example). If not, say "No behavior change."

     If the change is to the wire contract itself rather than to this server's
     implementation of it, the spec and conformance fixtures live in
     haverstack/core — link that PR here. -->

## Verification

<!-- What you ran, and anything you checked by hand.
     pnpm run format:check && pnpm run lint && pnpm typecheck && pnpm test -->

## Notes for reviewers

<!-- Optional: trade-offs, alternatives considered, follow-up work worth filing. -->
